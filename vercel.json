{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "pnpm run build",
        "installCommand": "pnpm install"
      }
    }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
} 